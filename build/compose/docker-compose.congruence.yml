version: '3.8'

services:

  phpcli:
    build:
      context: .
      dockerfile: build/docker/php/8.0/cli/Dockerfile
    image: phpcli
    container_name: phpcli-app

    working_dir: /var/www/
    volumes:
      - ./examples/congruence:/var/www

    networks:
      - appdata

  web:
    build:
      context: .
      dockerfile: build/docker/nginx/Dockerfile
    image: web
    container_name: web-app

    ports:
      - "80:80"
      - "443:443"

    volumes:
      - ./config/docker/nginx/sites:/etc/nginx/conf.d:rw
      - ./examples/congruence:/var/www

    networks:
      - appdata


networks:
  appdata:
    driver: bridge
